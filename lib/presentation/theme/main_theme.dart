import 'package:flutter/material.dart';

class AllTheme {
  final customColor = const Color(0xFFF68738);

  final cutomDarkColor = Color(0xFFFC841B);

  final blackColor = Colors.black;

  final blackHomeColor = Color.fromARGB(255, 29, 14, 14);

  final greyColor = Color.fromRGBO(190, 190, 190, 0.66);

  final buttonInactiveColor = Color.fromRGBO(29, 27, 32, 0.12);
}
